# Install all bash scripts in local/bin/ and add to path (includes todo.sh for TODO tmux sessions setup)

# Open a terminal (gnome-terminal) and change shortcut for zooming in from Ctrl-+ to Ctrl-=


